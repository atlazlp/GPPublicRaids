name: "${project.name}"
main: "${project.groupId}.${project.artifactId}.${project.name}"
version: "${project.version}"
api-version: "${project.api-version}"
author: "${project.developers[0].name}"
description: "${project.description}"
depend:
  - GriefPrevention
commands:
    claimallowpublicraids:
      description: Toggles whether raids can only be triggered by trusted players.
      usage: /ClaimAllowPublicRaids
      permission: griefprevention.claims
      aliases: claimallowpublicraids